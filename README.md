# ShaderForgeMetaPassRemover

Tools to remove Meta pass (light bounce coloring) in shaderforge automatically, which will break screen space shader if it's not removed manually (prior shader forge 1.35)
